machine:
  xcode:
    version: "6.3.1"
  environment:
    XCODE_PROJECT: platforms/ios
dependencies:
  override:
   - bower install
   - npm install
   - gulp
   - ./node_modules/.bin/phonegap run ios